命令格式：ADD table 字段 数据类型 约束条件；
命令用例：ADD 成绩 班级 String 00;
命令解释：添加一个字段名称为“班级”的字段在“成绩”这个表单里面，并写出这个字段的数据类型”string”和约束”00”。
相关提示：各条件间用空格隔开，数据类型和约束条件不能缺少，数据类型只有INT STRING DOUBLE三种，约束第一个字符表示主键约束是否存在，第二个字符表示非空约束是否存在（1表示该约束存在，0表示不存在）。